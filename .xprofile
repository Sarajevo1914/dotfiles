#!/bin/sh

# Ensure that xrdb has finished running before moving on to start the WM/DE.
[ -n "$xrdbpid" ] && wait "$xrdbpid"

if [ -f ~/.xinitrc ]; then
    source ~/.xinitrc
fi

/usr/lib/xdg-desktop-portal-gtk &

XDG_CURRENT_DESKTOP=GNOME /usr/lib/xdg-desktop-portal &

